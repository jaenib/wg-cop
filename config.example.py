"""Example configuration for the Telegram bot.

Copy this file to ``config.py`` and fill in your real values locally.
The ``config.py`` file is excluded from version control so secrets are not
accidentally committed.
"""

# Telegram bot token provided by BotFather
TOKEN = "your-bot-token"

# Numeric chat id of the group where the bot operates
GROUP_CHAT_ID = "your-group-chat-id"

# Telegram id of the user allowed to administer the bot
BOT_HANDLER_ID = "your-handler-id"

# Optional Telegram id for the chronicler feature (leave blank if unused)
CHRONICLER_ID = "your-chronicler-id"
